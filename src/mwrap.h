#include <string>
#include <vector>

std::vector<unsigned char> _thin(unsigned char *data,
                                int h,
                                int w,
                                const std::string & implementation_name);